# requirements.txt
# Full production stack for src/radar_har (PyTorch training/inference).
# For the dependency-light demo that runs without torch, see
# reference_pipeline/requirements.txt instead.

# --- core ML ---
torch>=2.2
torchvision>=0.17
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# --- explainability / analytics ---
matplotlib>=3.7
seaborn>=0.13
umap-learn>=0.5      # optional, falls back to t-SNE if absent

# --- MLOps ---
mlflow>=2.10
dvc>=3.40
joblib>=1.3
onnx>=1.15
onnxruntime>=1.17

# --- backend API ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9

# --- config / utils ---
PyYAML>=6.0
tqdm>=4.66

# --- testing ---
pytest>=8.0
pytest-cov>=4.1
httpx>=0.27
